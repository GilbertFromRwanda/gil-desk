# Rust
core/target/
desktop/native/target/
desktop/native/*.node
Cargo.lock

# Dev-only TLS identity generated by running core's host/client CLI
# binaries (see crypto/tls.rs docs — never a real trust mechanism)
core/nexdesk_host_cert.der
core/nexdesk_host_cert.der.key

# Go
backend/nexdesk-server
backend/*.exe

# Node / Electron
desktop/node_modules/
desktop/dist/
desktop/dist-electron/
desktop/release/
web/node_modules/
web/dist/

# C++
codec/build/

# General
.env
.env.*
*.log
.DS_Store
Thumbs.db
