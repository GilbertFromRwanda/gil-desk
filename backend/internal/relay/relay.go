// Package relay implements the fallback media relay: authentication,
// bandwidth accounting, connection quotas and observability (planner tasks
// G-20..G-24). Authenticate before allocating relay resources.
package relay
