// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v3.21.12
// source: nexdesk/v1/relay.proto

package nexdeskv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RelayService_Stream_FullMethodName = "/nexdesk.v1.RelayService/Stream"
)

// RelayServiceClient is the client API for RelayService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Byte relay for when a direct peer-to-peer connection isn't possible
// (planner Gate G3's "if direct fails -> relay" step, tasks G-20/G-21).
// Both sides of a session dial Stream, presenting the *same*
// RequestSession-issued session_token (RendezvousService, G-12) as gRPC
// metadata (key "x-nexdesk-session-token") on the call itself, not as a
// message — see RelayFrame's own doc comment for why that changed. The
// relay pairs the two streams by that token and forwards every frame
// byte-for-byte in both directions from then on. The token is the only
// authentication a peer needs to present here — it already proves the
// rendezvous server checked the pairing was authorized, so the relay
// doesn't need its own database round trip (same reasoning TokenIssuer's
// own doc comment gives for why it exists at all).
type RelayServiceClient interface {
	Stream(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[RelayFrame, RelayFrame], error)
}

type relayServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewRelayServiceClient(cc grpc.ClientConnInterface) RelayServiceClient {
	return &relayServiceClient{cc}
}

func (c *relayServiceClient) Stream(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[RelayFrame, RelayFrame], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &RelayService_ServiceDesc.Streams[0], RelayService_Stream_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[RelayFrame, RelayFrame]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RelayService_StreamClient = grpc.BidiStreamingClient[RelayFrame, RelayFrame]

// RelayServiceServer is the server API for RelayService service.
// All implementations must embed UnimplementedRelayServiceServer
// for forward compatibility.
//
// Byte relay for when a direct peer-to-peer connection isn't possible
// (planner Gate G3's "if direct fails -> relay" step, tasks G-20/G-21).
// Both sides of a session dial Stream, presenting the *same*
// RequestSession-issued session_token (RendezvousService, G-12) as gRPC
// metadata (key "x-nexdesk-session-token") on the call itself, not as a
// message — see RelayFrame's own doc comment for why that changed. The
// relay pairs the two streams by that token and forwards every frame
// byte-for-byte in both directions from then on. The token is the only
// authentication a peer needs to present here — it already proves the
// rendezvous server checked the pairing was authorized, so the relay
// doesn't need its own database round trip (same reasoning TokenIssuer's
// own doc comment gives for why it exists at all).
type RelayServiceServer interface {
	Stream(grpc.BidiStreamingServer[RelayFrame, RelayFrame]) error
	mustEmbedUnimplementedRelayServiceServer()
}

// UnimplementedRelayServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRelayServiceServer struct{}

func (UnimplementedRelayServiceServer) Stream(grpc.BidiStreamingServer[RelayFrame, RelayFrame]) error {
	return status.Error(codes.Unimplemented, "method Stream not implemented")
}
func (UnimplementedRelayServiceServer) mustEmbedUnimplementedRelayServiceServer() {}
func (UnimplementedRelayServiceServer) testEmbeddedByValue()                      {}

// UnsafeRelayServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RelayServiceServer will
// result in compilation errors.
type UnsafeRelayServiceServer interface {
	mustEmbedUnimplementedRelayServiceServer()
}

func RegisterRelayServiceServer(s grpc.ServiceRegistrar, srv RelayServiceServer) {
	// If the following call panics, it indicates UnimplementedRelayServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RelayService_ServiceDesc, srv)
}

func _RelayService_Stream_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(RelayServiceServer).Stream(&grpc.GenericServerStream[RelayFrame, RelayFrame]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RelayService_StreamServer = grpc.BidiStreamingServer[RelayFrame, RelayFrame]

// RelayService_ServiceDesc is the grpc.ServiceDesc for RelayService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RelayService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "nexdesk.v1.RelayService",
	HandlerType: (*RelayServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Stream",
			Handler:       _RelayService_Stream_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "nexdesk/v1/relay.proto",
}
