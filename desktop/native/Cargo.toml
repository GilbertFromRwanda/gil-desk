[package]
name = "nexdesk-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", default-features = false, features = ["napi4"] }
napi-derive = "2"
nexdesk-core = { path = "../../core/nexdesk-core" }

[build-dependencies]
napi-build = "2"
