# Kubernetes

Do not introduce Kubernetes manifests here until the Docker Compose deployment (planner Section 20) has proven containerized deployment, health checks, horizontal scaling, observability, and backup/restore in practice.
